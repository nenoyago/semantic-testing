name: ✏️ PR Title Check

on:
  pull_request:
    types: [opened, edited, reopened, synchronize, ready_for_review]
    branches: [main, stage]

permissions:
  issues: write
  pull-requests: write

jobs:
  validate-title:
    name: 📝 Validate PR Title
    uses: nenoyago/.github/.github/workflows/title-validation.yml@main
